# Database Configuration
DATABASE_URL=postgresql://mantis@localhost:5432/tcg_tracker

# JWT Configuration
# IMPORTANT: Generate a strong secret for production (e.g., openssl rand -base64 32)
JWT_SECRET=your-secret-key-change-in-production

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS and password reset links)
FRONTEND_URL=http://localhost:5173

# Email Configuration (Resend)
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key_here
# For testing, use Resend's verified test domain: onboarding@resend.dev
# For production, use your own verified domain: TCG Tracker <noreply@your-domain.com>
RESEND_FROM_EMAIL=onboarding@resend.dev
